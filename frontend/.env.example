# Frontend Environment Variables

# API URL - Leave empty for Docker deployment (uses Nginx proxy)
# For local development: http://localhost:8000
# For Docker: (empty - uses relative URLs)
VITE_API_URL=
